# springBootApiBaseExample
Basic Api fully implemented with swagger as UI, H2 db, jpa repository, 
This example implements a book list, fully operations supported,(GET,PUT,POST,UPDATE,DELETE)
with tests
